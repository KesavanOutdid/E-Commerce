version: '3.8'

services:
  seller:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecommerce-seller-standalone
    restart: unless-stopped
    ports:
      - "3003:3003"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:5000}
