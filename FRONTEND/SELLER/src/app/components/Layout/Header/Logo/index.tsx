import Link from "next/link";

const Logo: React.FC = () => {
    return (
        <Link href="/" className="text-white font-bold text-xl hover:opacity-90 transition-opacity">
            OUTDID E-Seller
        </Link>
    );
};

export default Logo;
